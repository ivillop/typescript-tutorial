"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        let name = "Raden Akbar";
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "eko";
        // isVip = 100;
    });
});
